<?xml version="1.0"?>
<block>
  <name>SI_canceller</name>
  <key>fullduplex_SI_canceller</key>
  <category>fullduplex</category>
  <import>import fullduplex</import>
  <make>fullduplex.SI_canceller($No_points)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>No_points</name>
    <key>No_points</key>
    <type>int</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in0</name>
    <type>complex</type>
  </sink>
  <sink>
    <name>in1</name>
    <type>complex</type>
  </sink>
  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out0</name>
    <type>complex</type>
  </source>
  <source>
    <name>out1</name>
    <type>complex</type>
  </source>
</block>
